# Implementation Plan: PC Builder Platform Expansion

## Overview

Incremental implementation of the PC Builder platform expansion. Tasks are ordered by dependency — database first, then backend services, then API routes, then frontend. Each task references specific requirements and design sections.

## Tasks

- [x] 1. Database schema migrations
  - [x] 1.1 Expand `components` table with new catalog fields
    - Add columns: `slug`, `brand`, `description`, `specs` (JSONB), `image_url`, `release_year`, `is_active`, `created_at`, `updated_at`
    - Add indexes: `components(slug)`, `components(category)`, `components(brand)`, `components(is_active)`
    - Write migration `006_expand_components.sql`
    - _Requirements: 1.1, 1.5_
  - [x] 1.2 Expand `retailers` table with scraping config fields
    - Add columns: `logo_url`, `country`, `is_active`, `scraping_interval_hours`, `last_scrape_at`, `last_scrape_status`, `notes`
    - Write migration `007_expand_retailers.sql`
    - _Requirements: 4.1_
  - [x] 1.3 Create `scraper_mappings` table
    - Columns: `id`, `component_id`, `retailer_id`, `product_url`, `product_identifier`, `created_at`, `updated_at`
    - Unique constraint on `(retailer_id, product_url)`
    - Write migration `008_create_scraper_mappings.sql`
    - _Requirements: 2.1_
  - [x] 1.4 Create `price_history` table
    - Columns: `id`, `component_id`, `retailer_id`, `price`, `in_stock`, `recorded_at`
    - Index on `(component_id, recorded_at DESC)`
    - Write migration `009_create_price_history.sql`
    - _Requirements: 3.1_
  - [x] 1.5 Create `preset_builds` and `preset_build_components` tables
    - `preset_builds`: `id`, `name`, `description`, `use_case`, `total_price_estimate`, `is_active`, `created_at`, `updated_at`
    - `preset_build_components`: `id`, `preset_build_id`, `component_id`, `category`, unique on `(preset_build_id, category)`
    - Write migration `010_create_preset_builds.sql`
    - _Requirements: 10.1_
  - [x] 1.6 Create `unmatched_listings` table
    - Columns: `id`, `retailer_id`, `product_url`, `scraped_name`, `scraped_price`, `scraped_at`, `status`, `linked_component_id`
    - Write migration `011_create_unmatched_listings.sql`
    - _Requirements: 2.2_
  - [x] 1.7 Create `admin_activity_log` table
    - Columns: `id`, `admin_id`, `action`, `entity_type`, `entity_id`, `details` (JSONB), `created_at`
    - Index on `created_at DESC`
    - Write migration `012_create_admin_activity_log.sql`
    - _Requirements: 7.5_
  - [x] 1.8 Create `refresh_tokens` table
    - Columns: `id`, `admin_id`, `token`, `expires_at`, `created_at`
    - Index on `token` and `expires_at`
    - Write migration `013_create_refresh_tokens.sql`
    - _Requirements: 11.2_


- [x] 2. Slug service and component catalog backfill
  - [x] 2.1 Create `backend/src/utils/slugify.ts`
    - `slugify(text: string): string` — converts brand + name to URL-safe slug (lowercase, hyphens, no special chars)
    - `generateUniqueSlug(brand: string, name: string, existingSlugs: string[]): string` — appends numeric suffix on collision
    - Unit tests for both functions
    - _Requirements: 1.3, 1.4_
  - [x] 2.2 Create `backend/src/services/slugService.ts`
    - `getUniqueSlug(brand: string, name: string): Promise<string>` — queries DB for existing slugs and returns unique one
    - _Requirements: 1.3, 1.4_
  - [x] 2.3 Write backfill script `backend/backfill_slugs.ts`
    - Generates slugs for all existing components that have no slug
    - Run once after migration 006
    - _Requirements: 1.3_

- [x] 3. Seed catalog data
  - [x] 3.1 Write `backend/seed_catalog.sql` with 150+ real-world components
    - CPUs (20): AMD Ryzen 5/7/9 AM5, Intel Core i5/i7/i9 LGA1700
    - Motherboards (18): B650, X670, B760, Z790 boards
    - GPUs (25): RX 6600–7900 XTX, RTX 3060–4090
    - RAM (20): DDR4 and DDR5 kits from Corsair, G.Skill, Kingston
    - Storage (20): NVMe SSDs and SATA SSDs from Samsung, WD, Seagate
    - PSUs (18): 550W–1000W from Corsair, be quiet!, Seasonic
    - Cases (15): ATX and mATX cases from NZXT, Fractal, Lian Li
    - Cooling (14): Air coolers and AIOs from Noctua, be quiet!, Corsair
    - All entries include correct `specs` JSONB, `slug`, `brand`, `release_year`
    - _Requirements: 1.6_
  - [x] 3.2 Write `backend/seed_retailers.sql` with Moroccan/regional retailers
    - Electro Bazar, Mattel, Mytek, Wiki, Tunisianet
    - Include `scraping_interval_hours`, `country`, `is_active`
    - _Requirements: 4.1_
  - [x] 3.3 Write `backend/seed_presets.sql` with 4 preset builds
    - Budget Gaming (gaming, ~8500 MAD)
    - Mid-Range Gaming (gaming, ~15000 MAD)
    - Workstation (workstation, ~25000 MAD)
    - Office PC (office, ~4500 MAD)
    - _Requirements: 10.1, 10.2_


- [x] 4. Update backend services
  - [x] 4.1 Update `componentService.ts` for new schema
    - `getComponents(filters)`: add `search`, `brand`, `page`, `limit` params; return `X-Total-Count`; exclude `is_active = false`
    - `getComponentById(id)`: return full component including `specs`, `slug`, `image_url`
    - `getComponentBySlug(slug)`: new method for detail page lookups
    - `createComponent(data)`: generate slug via `slugService`, insert with all new fields
    - `updateComponent(id, data)`: update including `updated_at`
    - `deactivateComponent(id)`: set `is_active = false`
    - Unit tests for all new/modified methods
    - _Requirements: 1.1, 1.7, 8.1, 13.1, 13.3_
  - [x] 4.2 Create `backend/src/services/priceHistoryService.ts`
    - `getPriceHistory(componentId, retailerId?, days?)`: query `price_history` with optional filters
    - `recordPriceChange(componentId, retailerId, price, inStock)`: insert into `price_history` only if price changed
    - Unit tests
    - _Requirements: 3.1, 3.2, 3.3_
  - [x] 4.3 Create `backend/src/services/retailerService.ts`
    - `getRetailers(includeInactive?)`: list retailers with price record counts
    - `getRetailerById(id)`: single retailer
    - `createRetailer(data)`: insert new retailer
    - `updateRetailer(id, data)`: update including `updated_at`
    - `deactivateRetailer(id)`: set `is_active = false`
    - Unit tests
    - _Requirements: 4.1, 4.3_
  - [x] 4.4 Create `backend/src/services/presetService.ts`
    - `getPresets(useCase?)`: list active presets with components; flag `incomplete` if any component is inactive
    - `getPresetById(id)`: single preset with full component data
    - `createPreset(data)`: insert preset and component links in a transaction
    - `updatePreset(id, data)`: update preset and replace component links in a transaction
    - `deletePreset(id)`: delete preset (cascades to `preset_build_components`)
    - Unit tests
    - _Requirements: 10.1, 10.5, 14.1_
  - [x] 4.5 Create `backend/src/services/adminService.ts`
    - `getDashboardStats()`: single query returning all dashboard stats
    - `getPriceUpdatesChart(days)`: price records updated per day
    - `getRecentActivity(limit)`: last N entries from `admin_activity_log`
    - `logActivity(adminId, action, entityType, entityId, details)`: insert into `admin_activity_log`
    - Unit tests
    - _Requirements: 7.1, 7.2, 7.3, 7.5_


- [x] 5. Update authentication system
  - [x] 5.1 Update `backend/src/routes/auth.ts`
    - `POST /api/auth/login`: return access token (15min JWT) in body + refresh token (7-day UUID) in `HttpOnly` cookie
    - `POST /api/auth/refresh`: validate refresh token from cookie, return new access token
    - `POST /api/auth/logout`: delete refresh token from DB, clear cookie
    - _Requirements: 11.1, 11.2, 11.3, 11.4, 11.5, 11.6_
  - [x] 5.2 Update `backend/src/middleware/auth.ts`
    - No functional change needed — still validates `Authorization: Bearer <token>`
    - Add `c.set('admin', decoded)` to attach admin info to context for activity logging
    - _Requirements: 11.3_
  - [x] 5.3 Write unit tests for new auth flows
    - Login returns access token + sets cookie
    - Refresh with valid cookie returns new access token
    - Refresh with expired/invalid cookie returns HTTP 401
    - Logout clears cookie and invalidates token
    - _Requirements: 11.2, 11.3, 11.4, 11.5_

- [x] 6. Update public API routes
  - [x] 6.1 Update `GET /api/components` in `routes/components.ts`
    - Add `search`, `brand`, `page`, `limit` query params
    - Return `X-Total-Count` header
    - Exclude inactive components
    - Return HTTP 400 if `limit > 100`
    - _Requirements: 9.1, 9.2, 13.3, 13.4, 13.5_
  - [x] 6.2 Update `GET /api/components/:id` in `routes/components.ts`
    - Return full component including `specs`, `slug`, `image_url`, `brand`, `description`
    - _Requirements: 9.3_
  - [x] 6.3 Add `GET /api/components/:id/price-history` in `routes/components.ts`
    - Accept `retailer_id` and `days` query params
    - Return price history array
    - _Requirements: 3.3_
  - [x] 6.4 Create `backend/src/routes/presets.ts`
    - `GET /api/builds/presets` with optional `use_case` filter
    - Return preset list with components; flag incomplete presets
    - _Requirements: 10.1, 10.5_
  - [x] 6.5 Create `backend/src/routes/health.ts`
    - `GET /api/health` returning `{ status: "ok", timestamp: "<ISO8601>" }`
    - _Requirements: 12.2_
  - [x] 6.6 Wire new routes in `backend/src/app.ts`
    - Register `presets` and `health` routes
    - _Requirements: 12.2_


- [x] 7. Create admin API routes
  - [x] 7.1 Create `backend/src/routes/admin/dashboard.ts`
    - `GET /api/admin/dashboard`: call `adminService.getDashboardStats()` and `getPriceUpdatesChart()` and `getRecentActivity()`
    - JWT-protected
    - _Requirements: 7.1, 7.2, 7.3_
  - [x] 7.2 Update `backend/src/routes/admin/components.ts`
    - `POST /api/admin/components`: generate slug, validate with Zod, insert, log activity
    - `PUT /api/admin/components/:id`: validate, update, log activity
    - `DELETE /api/admin/components/:id`: check for linked prices/mappings (HTTP 409 if found), delete, log activity
    - `POST /api/admin/components/import`: parse CSV/JSON, validate all rows, insert in transaction, return report
    - _Requirements: 6.1, 6.2, 6.3, 6.4, 6.5, 6.6, 6.7, 8.1, 8.2, 8.3, 8.4, 8.5, 8.6_
  - [x] 7.3 Create `backend/src/routes/admin/retailers.ts`
    - `GET /api/admin/retailers`: list with scraping stats
    - `POST /api/admin/retailers`: create retailer
    - `PUT /api/admin/retailers/:id`: update retailer
    - `DELETE /api/admin/retailers/:id`: deactivate (set `is_active = false`)
    - JWT-protected, log all write actions
    - _Requirements: 4.1, 4.3, 4.5_
  - [x] 7.4 Create `backend/src/routes/admin/scrapers.ts`
    - `POST /api/admin/scrapers/:retailerId/run`: trigger immediate scrape for one retailer (HTTP 409 if already running)
    - `POST /api/admin/scrapers/run-all`: trigger all active retailers sequentially
    - JWT-protected
    - _Requirements: 5.1, 5.2, 5.3, 5.6_
  - [x] 7.5 Create `backend/src/routes/admin/unmatched.ts`
    - `GET /api/admin/unmatched-listings`: list with optional `status` and `retailer_id` filters
    - `POST /api/admin/unmatched-listings/:id/link`: link to component, create `scraper_mappings` entry
    - `POST /api/admin/unmatched-listings/:id/dismiss`: set status to `dismissed`
    - JWT-protected
    - _Requirements: 2.3, 2.4, 5.5_
  - [x] 7.6 Create `backend/src/routes/admin/presets.ts`
    - `GET /api/admin/presets`: list all presets including inactive
    - `POST /api/admin/presets`: create preset with components in transaction
    - `PUT /api/admin/presets/:id`: update preset
    - `DELETE /api/admin/presets/:id`: delete preset
    - JWT-protected, log all write actions
    - _Requirements: 10.2_
  - [x] 7.7 Wire all new admin routes in `backend/src/app.ts`
    - Register dashboard, retailers, scrapers, unmatched, presets routes under `/api/admin`
    - _Requirements: 4.5_


- [x] 8. Update scraper system
  - [x] 8.1 Update `backend/scraper/aggregator.ts`
    - Look up `scraper_mappings` by `(retailer_id, product_url)` before inserting price
    - If mapping found: UPSERT `prices`, call `priceHistoryService.recordPriceChange()`
    - If no mapping: INSERT into `unmatched_listings` (skip if already exists)
    - Update `retailers.last_scrape_at` and `last_scrape_status` after each session
    - _Requirements: 2.1, 2.2, 3.2, 4.2_
  - [x] 8.2 Update `backend/scraper/scheduler.ts`
    - Read `scraping_interval_hours` per retailer from DB instead of hardcoded 24h
    - Skip retailers where `is_active = false`
    - _Requirements: 4.2, 4.4_
  - [x] 8.3 Add retry logic with exponential backoff to `backend/scraper/scrapers/baseScraper.ts`
    - Retry up to 3 times with delays of 2s, 4s, 8s on network timeout or HTTP error
    - Log each retry attempt at WARNING level
    - _Requirements: 14.4_
  - [x] 8.4 Update scraper unit tests for new aggregator behavior
    - Test: matched product → price UPSERTED and history recorded
    - Test: unmatched product → inserted into `unmatched_listings`
    - Test: retry logic fires on network error, succeeds on second attempt
    - Test: retry exhausted → error logged, scraper continues
    - _Requirements: 2.2, 14.4_

- [x] 9. Checkpoint — all backend tests pass
  - Run `bun test` and confirm all tests pass
  - Fix any regressions from schema or service changes


- [x] 10. Update React frontend (user-facing)
  - [x] 10.1 Add React Router to `frontend/`
    - Install `react-router-dom`
    - Create `frontend/src/router.tsx` with routes: `/`, `/components/:slug`, `/build`, `/presets`
    - Update `frontend/src/main.tsx` to wrap app in `<BrowserRouter>`
    - _Requirements: 9.5_
  - [x] 10.2 Create `frontend/src/components/ComponentPicker.tsx`
    - Searchable input with 300ms debounce
    - Filter chips: brand, socket, RAM type, price range
    - "Compatible only" toggle
    - Component list with thumbnail image (fallback to category icon)
    - Pagination: 20 items per page with prev/next controls
    - On select: emit selected component to parent
    - _Requirements: 9.1, 9.2, 9.4, 9.7_
  - [x] 10.3 Update `frontend/src/components/Configurator.tsx`
    - Replace dropdown selects with `ComponentPicker` component per slot
    - Show compact summary card (name, brand, image, lowest price) when component selected
    - _Requirements: 9.1, 9.6_
  - [x] 10.4 Create `frontend/src/components/PriceHistoryChart.tsx`
    - Install `recharts`
    - Line chart: X-axis = date, Y-axis = price (MAD), one line per retailer
    - Tooltip showing exact price and date on hover
    - Show "Price history not yet available" when fewer than 2 data points
    - _Requirements: 3.4, 3.5_
  - [x] 10.5 Create `frontend/src/pages/ComponentDetail.tsx`
    - Fetch component by slug: `GET /api/components/:id` (resolve slug to id first)
    - Fetch prices: `GET /api/components/:id/prices`
    - Fetch price history: `GET /api/components/:id/price-history`
    - Layout: image, name, brand, specs table, price comparison table, price history chart
    - "Add to Build" button
    - _Requirements: 9.3, 9.4, 3.4_
  - [x] 10.6 Create `frontend/src/pages/Presets.tsx`
    - Fetch `GET /api/builds/presets`
    - Display preset cards grouped by use case
    - "Load Build" button: loads all components into Configurator state and runs compatibility validation
    - _Requirements: 10.3, 10.4_
  - [x] 10.7 Update `frontend/src/api.ts`
    - Add `getComponentBySlug(slug)`, `getPriceHistory(id, params)`, `getPresets(useCase?)` functions
    - Add `search`, `brand`, `page`, `limit` params to `getComponents()`
    - _Requirements: 9.1, 3.3, 10.1_


- [x] 11. Build admin panel
  - [x] 11.1 Scaffold admin app
    - Create `admin/` directory with Vite + React + TypeScript
    - Install dependencies: `react-router-dom`, `recharts`
    - Create `admin/src/router.tsx` with all admin routes
    - Create `admin/src/api.ts` with auth-aware fetch wrapper (attaches Bearer token, handles 401 → redirect to login)
    - _Requirements: 11.1_
  - [x] 11.2 Create `admin/src/pages/Login.tsx`
    - Email + password form
    - On submit: call `POST /api/auth/login`, store access token in memory
    - Redirect to `/admin/dashboard` on success
    - Show error message on invalid credentials
    - _Requirements: 11.1, 11.2_
  - [x] 11.3 Create `admin/src/pages/Dashboard.tsx`
    - Fetch `GET /api/admin/dashboard` on mount
    - Stats cards: total components, active retailers, price records, unmatched listings
    - Bar chart: price updates per day (past 30 days) using Recharts
    - Recent activity feed: last 10 actions
    - Auto-refresh every 60 seconds
    - _Requirements: 7.1, 7.2, 7.3, 7.4, 7.5_
  - [x] 11.4 Create `admin/src/pages/Components.tsx` and `ComponentForm.tsx`
    - List view: search, filter by category/active status, sort controls
    - Actions: Edit, Deactivate/Activate, Delete (with confirmation dialog)
    - Create/Edit form: dynamic spec fields based on selected category, image URL field, auto-generated slug
    - _Requirements: 8.1, 8.2, 8.3, 8.4, 8.5, 8.6_
  - [x] 11.5 Create `admin/src/pages/BulkImport.tsx`
    - File upload (CSV or JSON)
    - Preview first 10 rows with validation status
    - Conflict resolution UI for duplicate slugs
    - Import button → show results (imported/skipped/failed counts)
    - _Requirements: 6.1, 6.2, 6.3, 6.4, 6.5, 6.6_
  - [x] 11.6 Create `admin/src/pages/Retailers.tsx`
    - List: name, active status, last scrape time, price records count
    - Create/Edit form: name, base_url, logo_url, country, scraping_interval_hours, notes
    - Deactivate/Activate toggle
    - _Requirements: 4.1, 4.3_
  - [x] 11.7 Create `admin/src/pages/Scrapers.tsx`
    - Status table: retailer, last run, status badge, prices updated, unmatched count
    - "Run Now" button per retailer
    - "Run All" global button
    - Log viewer: last 100 entries, filterable by retailer/severity/date
    - _Requirements: 5.1, 5.2, 5.3, 5.4_
  - [x] 11.8 Create `admin/src/pages/UnmatchedListings.tsx`
    - Table: retailer, scraped name, price, URL, date
    - "Link" button: opens modal with searchable component picker
    - "Dismiss" button
    - _Requirements: 2.3, 5.5_
  - [x] 11.9 Create `admin/src/pages/Presets.tsx`
    - List preset builds with use case badges
    - Create/Edit form: name, description, use case, component selectors per slot
    - Delete with confirmation
    - _Requirements: 10.2_
  - [x] 11.10 Implement token refresh in admin API client
    - On 401 response: call `POST /api/auth/refresh`
    - If refresh succeeds: retry original request with new token
    - If refresh fails: redirect to `/admin/login`
    - _Requirements: 11.3, 11.4_


- [x] 12. Deployment setup
  - [x] 12.1 Create `backend/Dockerfile`
    - Base image: `oven/bun:1.3-alpine`
    - Copy `package.json` and `bun.lock`, run `bun install --frozen-lockfile --production`
    - Copy source, expose port 3000, CMD `bun run src/server.ts`
    - _Requirements: 12.4_
  - [x] 12.2 Create `docker-compose.yml` at project root
    - Services: `postgres` (postgres:15-alpine), `backend` (custom Dockerfile), `nginx` (nginx:alpine)
    - Postgres mounts migrations as init scripts
    - Backend depends on postgres, mounts `frontend/dist`
    - Nginx serves frontend static files and proxies `/api` to backend
    - _Requirements: 12.4_
  - [x] 12.3 Create `nginx.conf`
    - Serve frontend static files at `/`
    - Proxy `/api` to `backend:3000`
    - Serve admin panel at `/admin`
    - `try_files` for SPA routing
    - _Requirements: 12.4_
  - [x] 12.4 Update `backend/.env.example`
    - Add all new environment variables: `DB_NAME`, `DB_USER`, `DB_PASSWORD`, `JWT_SECRET`, `PORT`, `NODE_ENV`, `SERVE_STATIC`
    - Add inline comments explaining each variable
    - _Requirements: 12.1, 12.5_
  - [x] 12.5 Update `backend/src/server.ts` for static file serving
    - When `NODE_ENV=production` and `SERVE_STATIC=true`, serve `frontend/dist` via `serveStatic`
    - _Requirements: 12.6_

- [x] 13. Property-based tests for expansion features
  - [x]* 13.1 Write property test — slug uniqueness
    - For any set of component names, assert all generated slugs are unique
    - _Requirements: 1.3, 1.4_
  - [x]* 13.2 Write property test — pagination correctness
    - For any `page` and `limit`, assert returned items match expected offset slice and `X-Total-Count` is accurate
    - _Requirements: 13.3, 13.4, 13.5_
  - [x]* 13.3 Write property test — price history insertion
    - For any sequence of price updates, assert `price_history` only gains a new row when price actually changes
    - _Requirements: 3.2_

- [x] 14. Final checkpoint — all tests pass
  - Run `bun test` in backend
  - Run frontend tests
  - Verify Docker Compose builds and starts cleanly
  - Verify `GET /api/health` returns 200
  - Verify seed data loads correctly

## Notes

- Tasks marked `*` are optional
- All backend files use TypeScript with ESM imports
- Run tests via WSL2: `wsl -d Ubuntu -- bash -c "cd /mnt/c/Headquarters/Projects/PcBuilder/backend && ~/.bun/bin/bun test 2>&1"`
- Admin panel is a separate Vite app in `admin/` directory
- Database transactions required for: bulk import, preset creation/update, scraper mapping creation
- Slug backfill (task 2.3) must run after migration 006 before any new components are created
